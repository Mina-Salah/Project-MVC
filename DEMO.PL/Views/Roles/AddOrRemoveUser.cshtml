@model List<UserInRoleModel>
@{
    ViewData["Title"] = "AddOrRemoveUser";
    var roleId = ViewBag.RoleId;
}

<h1>Add Or Remove User</h1>

<form asp-action="AddOrRemoveUser" asp-route-roleId="@roleId">

    @for (int i = 0; i < Model.Count; i++)
        {
            <div class="form-group">
                <input asp-for="@Model[i].IsSelected" />
                <label asp-for="@Model[i].IsSelected">@Model[i].UserName</label>
      
              </div>

              <input type="hidden" asp-for="@Model[i].UserId" />
              <input type="hidden" asp-for="@Model[i].UserName" />
        }

   <div class="my-3">
       <input type="submit" value="Update" class="btn btn-success " />
        <a asp-action="Update" asp-controller="Roles" asp-route-id="@roleId" class="btn btn-danger">Cancel</a>
    </div>
</form>

